[2026-04-14] - Homepage Shortcodes Redesign - Modernized the Top Universities carousel and applied a grid layout to the Trending Study Areas to match premium styling and prevent vertical stacking.   Added spacing between Top University carousel items and enforced circular clipping for Popular Destination flags.

## [Unreleased]

### Changed

- [2026-04-14] - Country Archive & FAQ Fixes - Fixed PHP warning "Undefined variable $university" on line 251 of `ProgramArchive.php` by initializing the variable before the search block. Added complete missing CSS for the `ProgramArchivePage-cities-*` classes (cities were rendering as unstyled text links). Added full FAQ accordion styles (`programPage-faq-*`) for the GEO-optimized FAQ section on single program pages: category tabs, collapsible items, chevron animation, answer fade-in, load-more button, loading spinner, and mobile responsive overrides. Added complete missing CSS for header controls (search bar, sort dropdown, export PDF button) and pagination footer (prev/next buttons, page numbers, show per-page dropdown) — both were rendering as raw unstyled HTML with default browser borders.
- [2026-04-14] - Grid & Filter UX Improvements - Fixed program cards grid layout (first card was full-width, rest were 2-up) by switching from flex to `display: grid; grid-template-columns: repeat(2, 1fr)`. Added "Show more" toggle to Degree Level filter (shows Bachelor/Master/PhD by default) and Language filter (shows English/Turkish by default). Added missing CSS for filter sidebar components: duration/scholarship pill buttons, price range inputs, and the show-more toggle button.

- [2026-04-14] - Homepage Shortcodes Redesign - Modernized the visual design of the `Top Universities` carousel, `Trending Study Areas`, and `Popular Student Destinations` shortcodes. Applied the `sit-ui-card` design system with glassmorphic elements, refined spacing, hover elevation effects, and clean grid layouts. The university attributes in the carousel now display as sleek pill tags rather than text rows, and trending/country grids have been restructured for better responsiveness and visual hierarchy. Added spacing between Top University carousel items and enforced circular clipping for Popular Destination flags.
- [2026-04-14] - Search Results Page UI/UX Fix & Redesign - Fixed completely broken search results page layout by loading the missing `filter-styles.css` stylesheet (was only loaded in campus page). Added comprehensive inline CSS for the results header bar (sr-* design system), program card grid (ProgramBoxUni-* styles), pagination, export modal, and related searches section. Page now renders with a proper sidebar+grid layout: sticky filter sidebar on the left with degree level, duration, language, price, and university filters; 2-column program card grid on the right with image headers, badges, attribute grids, and red CTA buttons. Fully responsive with mobile filter toggle.
- [2026-04-14] - University Page UI/UX Redesign - Complete visual overhaul of the single university page (`single-university.html.php`). Migrated from legacy `uni-*` CSS to new `up-*` design system matching the program page's `pp-*` aesthetic. New clean white hero with pill badges, color-coded stat tiles (Type, Founded, Location, QS Ranking, Accommodation), gradient CTA banner, premium sidebar with sticky navigation and help/callback cards. Removed ~120 lines of orphaned `uni-*` responsive breakpoints. Fixed PHP parse error caused by redundant `if(!empty($other_uni))` check. Fully responsive across all breakpoints.
- [2026-04-14] - Program Page UI/UX Redesign - Complete visual overhaul of the single program page (`single-program.html.php`). Replaced dark hero with clean white layout featuring pill badges, modernized stat grid with color-coded icon tiles, added standalone tuition comparison card with savings badge, implemented gradient CTA section, redesigned sidebar with sticky navigation and help cards. Migrated all CSS from `programPage-*` namespace to `pp-*` namespace, reducing CSS by ~6,800 lines. Added entry/exit animations and improved mobile responsiveness across all breakpoints.
- [2026-04-14] - University Programs List View - Converted the programs section on the university detail page from a 2-column image card grid to a compact, scannable list layout. Each program row now shows a color-coded degree level badge (Bachelor=indigo, Master=amber, PhD=pink, Associate=green), clickable title, inline metadata tags (duration, language, mode, intake) with icons, right-aligned tuition price, and a compact "View" CTA. Increased items per page from 2 to 10 for faster browsing. Fully responsive with stacked layout on mobile. Updated JavaScript selectors for search/pagination compatibility.
- [2026-03-10] - University Programs Redesign - Rewrote `[university_programs]` output to display programs in a premium list view rather than grid cards. Added instant client-side search functionality layered over the existing tab filter system.

### Fixed

- [2026-04-14] - Program Archive Search Page Fixes - Fixed completely broken program list formatting on the Country search page (`program-archive.html.php`). Repaired the PDF export modal (`#exportModal` and `#expoting-download`) which was visible by default and polluting the layout. Fixed the "Total Programs: 0" count error by substituting `count($pdf_program)` with actual query result counts. Ensured `$pdf_program` defaults to the current page `$programs` list to avoid empty table renders when the export button is clicked without a URL reload. Finally, injected fallback labels ("Contact University", "Not specified", "Full-time") for fee, duration, study mode, and intake sections on program cards (`program-box-uni.html.php` and List View) to eliminate empty text voids when metadata is absent.

- [2026-03-10] - Plugin Security & Performance Overhaul - Comprehensive review of `sit-search-plugin`: fixed SQL injection in university LIKE search (esc_like), eliminated N+1 query in `ajax_load_universities` via batch post lookup, added 300ms debounce to search inputs, added transient caching for filter dropdowns, replaced `uniqid()` cache-busting with `filemtime()` versioning, removed dead `get_data()` function, removed duplicate `UPD_AjaxHandler` instantiation, removed fake random stats from university cards, removed debug red borders from CSS, added lazy loading to university images, added `line-clamp` CSS compat property.

- [2026-03-10] - University Programs Tabs Fix - Fixed `[university_programs]` shortcode tabs not filtering programs by using Javascript `setProperty` to properly override `!important` CSS visibility rules.
- [2026-03-10] - Program Search Shortcode Fix - Fixed `[program_search]` shortcode not returning results due to unconditional database prepare statements with empty parameters and removed conflicting legacy JavaScript event listeners overriding the search handler.
- [2026-03-09] - University Grid AJAX Error - Resolved "Network error" (403 Forbidden) on cached pages by relaxing nonce verification for read-only university search.
- Improved AJAX error handling in `university-grid.js` to provide more helpful instructions (e.g., "session expired").

### Changed

- [2026-03-09] - University Grid Redesign - Implemented a premium, modern design with soft shadows, 16px border-radius, vibrant red accents, and improved typography.
- Refactored `UniversityGrid.php` card structure for cleaner HTML and better CSS targeting.
- Removed redundant manual style injection from `university-grid.js`.
  [2026-03-02] - AI Search Stability & UI Polish - Fixed issues with AI search returning no results for natural language queries like 'computer engineering in Istanbul'. Increased cURL timeout to 8s, implemented a smart local fallback with keyword mapping, and differentiated cache TTLs to ensure freshness. Added taxonomy-based search (speciality terms) to the query engine. Updated 'Recent Searches' font colors to black for better visibility.
  [2026-03-01] - Plugin Security & Performance Overhaul - Addressed critical vulnerabilities including hardcoded OpenAI API keys, insecure debug modes, and unrestricted SVG uploads. Dramatically improved performance by eliminating N+1 queries in the search loop, implementing REST response caching, and removing `session_start` to ensure compatibility with object cache. All REST endpoints now have strict parameter sanitization.
  [2026-03-01] - AI Search Smart Filtration - Expanded the `AiSearchHelper` to implicitly extract filter variables (Degree, Language, City, Country) from human strings and automatically inject them into the system `tax_query`.
  [2026-02-28] - AI Search v2 (Server-Side Redesign) - Moved all AI search logic from frontend JavaScript to server-side PHP. New AiSearchHelper service calls OpenAI with 24h caching. Works with all search inputs automatically. Eliminated JS timing/loading issues.
  [2026-02-20] - AI Search Module - Implemented `[sit_ai_search]` shortcode powered by OpenAI (gpt-4o-mini), including admin settings page, index caching, frontend sorting, and debounced REST endpoints.
  [2026-02-20] - All Search Fields Optional - Removed Javascript validation blocking the search bar form if some fields are not provided. Users can now search with any combination of criteria.
  [2026-02-04] - Fix Country Filtering - Refactored program search to filter by University's country instead of Program's country tag, resolving incorrect results for North Cyprus.
  [2026-02-05] - Country Filter & Search Bar Fixes - Fixed `FilterSort.php` to correctly filter programs by university country (resolving North Cyprus issue) and corrected `selected` attribute logic in `search-bar.html.php`.
